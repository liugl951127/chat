server:
  port: 8082

spring:
  application:
    name: fin-chat-center
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}

fin:
  hash-chain:
    batch-size: 100

logging:
  level:
    com.fin: DEBUG
